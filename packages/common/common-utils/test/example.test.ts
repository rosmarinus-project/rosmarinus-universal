import { describe, afterEach, beforeEach, test } from '@jest/globals';

describe('example', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('case', () => {
    console.log('hello test');
  });
});
